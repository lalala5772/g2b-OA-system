fastapi==0.115.0
uvicorn[standard]==0.31.0
python-multipart==0.0.12
python-docx==1.1.2
pdfplumber==0.11.4
pydantic-settings==2.5.2
anthropic==0.120.0
sentence-transformers==3.2.1
requests==2.32.3

# Optional: enables .hwp (legacy) text extraction via `hwp5txt`.
# Not required to install — hwp_parser.py falls back to a
# "convert to PDF and re-upload" message when this isn't available.
# pyhwp==0.1b15
